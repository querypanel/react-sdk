node_modules/
.DS_Store

# Build and cache output
.turbo/
dist/
coverage/
storybook-static/

# Env files
.env
.env.local
.env.*.local

# Workspace local artifacts
.worktrees/
.migration-backup/

# Keep nested .git dirs out of root tracking if present during migration.
admin/.git
api/.git
database/.git
admin/packages/react-sdk/.git
admin/packages/node-sdk/.git
